services:
  remotepanel:
    build:
      context: .
    environment:
      DATA_DIR: /data
      APP_SECRET_KEY: ${APP_SECRET_KEY:-}
      COOKIE_SECURE: ${COOKIE_SECURE:-false}
      TRANSFER_CHUNK_SIZE: ${TRANSFER_CHUNK_SIZE:-67108864}
      TRANSFER_PREFETCH_CHUNKS: ${TRANSFER_PREFETCH_CHUNKS:-16}
      TRANSFER_PARALLEL_FILES: ${TRANSFER_PARALLEL_FILES:-2}
      TRANSFER_FILE_STREAMS: ${TRANSFER_FILE_STREAMS:-16}
      TRANSFER_FILE_STREAM_MIN_SIZE: ${TRANSFER_FILE_STREAM_MIN_SIZE:-1073741824}
      TRANSFER_MEMORY_TRIM_BYTES: ${TRANSFER_MEMORY_TRIM_BYTES:-21474836480}
      TRANSFER_MEMORY_TRIM_PAUSE_SECONDS: ${TRANSFER_MEMORY_TRIM_PAUSE_SECONDS:-1}
      SMB_REQUIRE_SIGNING: ${SMB_REQUIRE_SIGNING:-true}
      SMB_AUTH_PROTOCOL: ${SMB_AUTH_PROTOCOL:-negotiate}
    volumes:
      - remotepanel-data:/data
    ports:
      - "${APP_PORT:-8080}:8000"
    restart: unless-stopped

volumes:
  remotepanel-data:
